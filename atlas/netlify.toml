# Netlify deployment for Atlas (standalone static Next.js export in /atlas).
# Create a separate Netlify site with "Base directory" set to `atlas`.
# The site is fully static — no Next.js runtime or plugin needed.

[build]
  command = "npm run build"
  publish = "out"
